server:
  workers: 2

additionalConfigProperties:
  - http-server.process-forwarded=true

coordinator:
  resources:
    requests:
      memory: 512Mi
      cpu: 250m
    limits:
      memory: 1Gi
      cpu: 500m

worker:
  resources:
    requests:
      memory: 512Mi
      cpu: 250m
    limits:
      memory: 1Gi
      cpu: 500m

service:
  type: ClusterIP

additionalCatalogs:
  iceberg: |-
    connector.name=iceberg
    iceberg.catalog.type=rest
    iceberg.rest-catalog.uri=http://nessie.k8sdatalab.com/iceberg/v1
    iceberg.rest-catalog.warehouse=s3://datalake/warehouse/

    # MinIO comme stockage S3
    fs.native-s3.enabled=true
    s3.endpoint=http://minio-api.k8sdatalab.com
    s3.aws-access-key=SMhhxFUV4chm3gwjPsea
    s3.aws-secret-key=xbTURiHNZq6yXUjfRJTPKB6aYybHZQa1kecmB1Ms
    s3.path-style-access=true
    s3.region=us-east-1
